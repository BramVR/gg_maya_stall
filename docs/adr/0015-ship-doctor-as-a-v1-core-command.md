# Ship Doctor As A V1 Core Command

Maya Stall will ship `doctor` as a v1 core command, with local/profile checks, exact-host checks, and optional Scenario checks. The command should follow Crabbox's layered doctor style by naming the failing layer and giving a practical repair hint instead of returning one opaque readiness failure.
