# Support GitHub And GitLab Review Publishing

Maya Stall v1 will publish Evidence Bundles through a platform-neutral Review Comment model with thin GitHub and GitLab adapters. The core publisher should render reusable markdown and handle bundle manifests independently from the hosting platform so GitHub PRs and GitLab MRs share the same evidence structure.
