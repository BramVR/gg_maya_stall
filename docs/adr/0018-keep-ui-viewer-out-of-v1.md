# Keep UI Viewer Out Of V1

Maya Stall v1 will provide screenshot and recording commands but no built-in UI viewer. Crabbox includes VNC and WebVNC because it manages general desktop leases and a shared viewing portal; Maya Stall can rely on existing host viewing tools for now and add a separate viewer command later only when the Session Broker exposes a stable viewer path.
