# Ship An Optional Maya Python Helper

Maya Stall will ship an optional tiny `maya_stall` Python helper for Scenario scripts running inside Maya. Crabbox leaves helpers to the remote command, but Maya Stall can reduce repeated JSON boilerplate by providing convenience functions for writing Scenario Results while still allowing scripts to emit the protocol manually.
