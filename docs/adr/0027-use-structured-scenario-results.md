# Use Structured Scenario Results

Maya Stall Scenarios will return a structured Scenario Result, preferably as JSON in a path supplied by the CLI. Command exit status and Maya exceptions provide coarse pass/fail, while the Scenario Result carries assertions, measurements, plugin load details, and produced outputs for validators and review evidence.
