# Use Crabbox-Style Stop Policy

Maya Stall will use a Crabbox-style Stop Policy: default Fresh Runs collect evidence and clean up, local users can keep a failed session for debugging, and explicit stop-after behavior can override the default. Kept Sessions must be visible in status output and retain enough commands to attach, collect more evidence, or stop the session.
