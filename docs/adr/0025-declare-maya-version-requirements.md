# Declare Maya Version Requirements

Maya Stall Scenarios may declare a Maya Version Requirement, and Host Health will verify that the chosen Maya Host has a compatible Autodesk Maya installation. This extends Crabbox's target/capability model with a Maya-specific compatibility check because plugin loading and UI behavior can depend strongly on Maya version.
