# Ship Evidence Publishing In V1

Maya Stall v1 will include Evidence Bundle publishing because review workflow needs durable, shareable visual proof, not just local files or CI artifacts. The implementation should still keep artifact collection separate from publishing so runs can produce a complete local bundle first and then publish it to the configured workflow target.
