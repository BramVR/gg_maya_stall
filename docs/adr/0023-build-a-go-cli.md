# Build A Go CLI

Maya Stall will be implemented as a Go CLI, matching Crabbox's single-binary operator experience. This fits CI and local use, avoids requiring Node or Python runtimes on runners, and gives the tool strong standard-library support for SSH-adjacent process, filesystem, and artifact workflows.
