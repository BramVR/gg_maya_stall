# Default To Fresh Maya UI Sessions

Maya Stall will default to a Fresh Run and reserve Debug Attach for explicit local investigation. This favors repeatable CI evidence over speed, while still leaving a deliberate path for inspecting broken Maya state.
