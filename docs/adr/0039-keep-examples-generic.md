# Keep Examples Generic

Maya Stall examples will demonstrate reusable Maya plugin and Scenario patterns without baking in `gg_klv_push` behavior. Crabbox keeps its examples provider- and workflow-focused rather than product-specific; Maya Stall should do the same so consuming repos own their own `.maya-stall.yaml` and domain-specific payloads.
